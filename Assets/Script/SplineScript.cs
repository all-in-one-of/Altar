using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SplineScript : MonoBehaviour {
	public Vector3 splinePosition;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
//		splinePosition = GameObject.BezierPath.GetPositionByDistance (Time.time);
//		PlayerScript splineScript = thePlayer.GetComponent<PlayerScript>();
	}
}
